# Workspace Agent Bootstrap

This repository contains an AI Coding Workspace whose content root is `workspace-template/`.

Before working, read `workspace-template/.workspace/manifest.json`, then follow its `entrypoints.agentProtocol` and existing Harness. Treat those files as the source of truth; do not copy their rules into this bootstrap.
