# UI/UX Memory

## Product Summary

- 产品：AI Coding Workspace，本地优先的 Markdown 项目知识空间。
- 平台：桌面 Web，兼顾平板与移动端阅读。
- 核心任务：通过知识树定位文档，在稳定、低干扰的阅读区内阅读和维护项目知识。

## Target Users and Jobs

- 主要用户：产品、设计与研发人员。
- 主要任务：快速定位、阅读、编辑和组织项目文档。
- 成功时刻：用户无需离开工作区即可找到可信文档并完成更新。

## Brand Personality

- 关键词：Minimal / Clean / Precise / Engineering。
- 感受：安静、克制、专业、高信息密度但不拥挤。
- 避免：大面积渐变、装饰性卡片、强阴影、夸张动画和高饱和色块。

## Design Method Blend

- 主方法：Utility Command Center。
- 辅助方法：Technical Authority、Premium Restraint。
- 原因：产品以高频知识操作为主，需要稳定结构、清楚状态和克制视觉。

## Existing Branding Baseline

- 配色：Zinc/Neutral 表面与文字，Indigo 仅用于选中、焦点和主操作。
- 字体：Inter、苹方、微软雅黑及系统无衬线；代码使用系统等宽字体。
- 布局：50px 顶栏 + 知识树 / 文档 / 文档导航三栏；文档为视觉主体。
- 组件：细边框、6–8px 圆角、少量柔和阴影、Lucide 图标。
- 基线文件：`src/styles.css`、`WorkspaceShell.vue`、`KnowledgeSidebar.vue`、`DocumentView.vue`。

## Design Tokens

- 颜色：使用 `src/styles.css` 中的 `--background`、`--surface*`、`--border*`、`--text*`、`--accent*` 与 `--danger`。
- 间距节奏：以 4px 为基础，常用 8 / 12 / 16 / 24 / 32px。
- 圆角：控件 5–7px，弹层 8px，状态标签使用全圆角。
- 阴影：仅弹层、抽屉使用 `--shadow`；常规面板依靠 1px 边框分层。
- 动效：120–200ms ease-out；拖拽过程关闭布局过渡；遵循 `prefers-reduced-motion`。

## Component Rules

- 按钮：默认透明或中性表面；图标按钮必须有可访问名称和清晰焦点环。
- 面板：不使用卡片化堆叠；侧栏以边框与背景层级区分。
- 导航：当前文档用淡色背景与 2px Accent 指示线；常用操作保持就近可见。
- 弹层：使用现有 Dialog / Dropdown，支持 Escape 与焦点恢复。
- 可调整侧栏：桌面端知识侧栏宽度为 220–360px，默认 264px；使用右侧分隔条拖拽，双击恢复，键盘方向键可调；1080px 以下不启用。
- 知识树排序：普通知识文件可拖拽到节点前后调整顺序，拖到文件夹中心表示移入；拖拽时使用 2px Accent 插入线或淡色文件夹目标态。菜单提供上移、下移、移到上一级和移动到文件夹作为可访问替代；`.workspace` Harness 不允许移动。
- 设置模块：Workspace 级设置从知识侧栏底部统一进入；内部使用 196px 紧凑导航组织 Skill、Role、Agent 与 Sync，内容使用单一主标题、分组标题与分隔线列表，不做 Dashboard 或卡片墙。
- Workspace 初始化：Manifest 项目名称为空时使用不可跳过的两步轻量向导；第一步收集名称、简介与语言并预览固定 ID，第二步从真实 Role 定义选择工作角色并启用对应 Space 写入权限，也可明确跳过后在设置中选择。
- Agent 设置：以只读、扁平的协议列表展示统一入口、九项核心协议与 Handoff 边界；界面直接读取 `.workspace/agents/protocol.json`，不提供编辑、删除或禁用操作。
- 快速入门：右上角提供稳定系统入口，页面以长文档式分节、扁平列表和少量强调区解释 Base Template；内容读取 `.workspace/base-template/` 的只读版本化数据，并与当前项目“开始阅读”严格分离。
- 文档 Minimap：锚定知识侧栏右边界后 7px，并随 220–360px 侧栏拖拽同步移动；在工作区垂直居中，默认显示统一 6px 低对比短线且无容器背景/边框，Hover/Focus 以当前条为显著波峰联动四级相邻条展开；与右侧目录共享 Heading 数据及滚动高亮，移动端隐藏。
- 文档位置：每个 Markdown 文档标题上方显示 Workspace 相对路径面包屑，并提供一键复制精确路径；使用低对比等宽字体，服务于人和 Agent 的文档定位。

## Interaction and State Rules

- Hover 只提升表面或文字层级，不使用位移。
- Focus-visible 使用 2px Accent 轮廓，不能仅依赖颜色表达状态。
- 文件操作使用轻量 Toast；删除必须确认；未保存离开必须提醒。
- GitHub 同步面向非技术角色使用“获取最新内容 / 查看本次修改 / 保存并上传”；Repository、Remote、Branch、Commit 与原始 Git 错误仅在“高级信息”中按需展开。
- 同步必须覆盖未连接、待首次上传、本地修改、同步中、已同步、远程更新、冲突与失败状态；错误先说明用户下一步，技术详情与凭据分离。
- 动画无 bounce，常规时长不超过 200ms。

## Accessibility Rules

- 目标：WCAG 2.2 AA。
- 所有图标操作必须有 `aria-label`；可拖拽分隔条使用 `role="separator"` 并支持键盘。
- 键盘顺序与视觉顺序一致；焦点态清晰；减少动效偏好必须生效。
- 桌面精确操作目标可小于 44px，但移动端主要操作优先达到 44px。

## Content and Microcopy Voice

- 中文为主，简洁、直接、面向任务。
- 操作词使用“新建文档”“保存”“删除”等明确动词。
- 错误信息说明原因与可恢复动作，不展示原始内部错误。

## Responsive Rules

- 1440px：完整三栏；知识侧栏允许 220–360px 调整。
- 1024px：隐藏右侧常驻面板，知识侧栏固定为 238px，正文优先。
- 768px 及以下：知识树和文档导航改为抽屉。
- 375px / 320px：单栏文档阅读，顶栏隐藏非必要文字。

## Do / Don't

### Do

- 优先保证正文宽度、可读性和键盘操作。
- 延续现有变量、密度、边框和状态模式。

### Don't

- 不让辅助面板覆盖桌面正文或无限扩张。
- 不新增与现有视觉语言冲突的颜色、阴影或大幅动画。

## Design Decision Change Log

| 日期 | 决策 | 原因 | 范围 |
|---|---|---|---|
| 2026-09-22 | 建立现有产品 UI/UX 基线 | 保证后续优化延续当前 Workspace 视觉语言 | 全局 |
| 2026-09-22 | 桌面知识侧栏支持 220–360px 可访问式拖拽调整 | 适应不同目录深度，同时保护正文最小宽度 | Workspace Shell |
| 2026-09-22 | Skill 设置使用侧栏工具入口与扁平能力列表 | 让人和 Agent 共用一个克制、可扩展的能力索引 | Skill Settings |
| 2026-09-22 | 知识树使用持久化顺序与明确拖放目标 | 支持长期知识结构重组，并保护 Workspace Harness 稳定路径 | Knowledge Tree |
| 2026-09-23 | 正文左侧增加共享 TOC 数据的轻量章节 Minimap | 提供低干扰的快速章节定位，同时保留完整右侧目录 | Document |
| 2026-09-23 | 将 Skill 设置扩展为统一 Workspace 设置模块 | 集中呈现真实 Skill/Role 数据，并为 Agent 与同步保留稳定入口 | Settings |
| 2026-09-23 | 文档 Minimap 移至知识树边界并加入邻近波形反馈 | 贴近 Codex 式边界导航，在不占正文宽度的前提下强化章节定位 | Document Navigation |
| 2026-09-23 | GitHub 同步改为面向任务的三步协作流程 | 降低产品、设计、测试和运营角色的 Git 术语负担，同时保留开发人员诊断入口 | Settings / Sync |
| 2026-09-24 | Agent 设置改为统一协作协议只读视图 | 让 Human 与不同 Coding Agent 读取同一份协议，避免 UI 与系统规则漂移 | Settings / Agent Protocol |
| 2026-09-24 | 新增 Base Template V1 快速入门只读页面 | 帮助普通用户理解真实功能、Harness、跨 Agent 协作和当前边界，同时保持项目介绍独立 | Workspace Header / Getting Started |
| 2026-09-24 | 项目名称为空时启用首次初始化弹窗并锁定 Workspace ID | 避免未命名项目直接进入工作区，并确保后续系统引用使用稳定机器标识 | Workspace Initialization / Manifest |
| 2026-09-24 | 首次保存一次生成项目首页、索引、Project Harness、空 Active Roles 与首个 Release | 让未初始化模板保持无业务数据，同时保证初始化完成后立即具备完整工作上下文 | Workspace Initialization / Base Template |
| 2026-09-24 | 初始化改为“项目信息 → 工作角色”的两步向导 | 在进入 Workspace 前建立可选职责上下文，同时保留稍后设置的低阻力路径 | Workspace Initialization / Roles |
